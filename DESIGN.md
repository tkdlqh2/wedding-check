---
omd: "0.1"
brand: "웨딩체크 (웨딩홀 스캔 오퍼레이터 인수인계 시스템, 가칭 — 확정 전)"
bootstrapped_from: ichef
bootstrapped_at: "2026-07-23"
tokens:
  source: reference-derived (ichef), unchanged (no delta_set overrides requested)
  colors:
    primary: "#e8552d"
    primary-hover: "#d14a26"
    accent-tint: "#fdede7"
    canvas: "#ffffff"
    surface: "#f7f7f7"
    surface-hover: "#efefef"
    foreground: "#1f1f1f"
    body: "#555555"
    muted: "#888888"
    disabled: "#bcbcbc"
    hairline: "#e6e6e6"
    border: "#d4d4d4"
    success: "#1fa463"
    warning: "#f5a623"
    error: "#e0353b"
    info: "#2b82e0"
  typography:
    family: { sans: "Pretendard, Apple SD Gothic Neo, Malgun Gothic", mono: "SFMono-Regular" }
    hero:      { size: 34, weight: 700, lineHeight: 1.15, use: "관리자 화면 대시보드 헤드라인" }
    heading:   { size: 22, weight: 700, lineHeight: 1.25, use: "카드 / 섹션 제목" }
    total:     { size: 24, weight: 700, lineHeight: 1.20, use: "인사이트 반복 횟수, 예식 건수 등 스캔 가능한 숫자" }
    pos-tile:  { size: 17, weight: 600, lineHeight: 1.30, use: "체크리스트 항목 / 질의 카드, 큰 탭 텍스트" }
    body:      { size: 15, weight: 400, lineHeight: 1.50, use: "본문, 설명" }
    button:    { size: 16, weight: 600, lineHeight: 1.20, use: "버튼 라벨" }
    caption:   { size: 12, weight: 400, lineHeight: 1.40, use: "캡션, 메타데이터" }
  spacing: { xs: 4, sm: 8, md: 12, base: 16, lg: 24, xl: 48, xxl: 80 }
  rounded: { sm: 4, md: 8, lg: 12, full: 9999 }
  shadow:
    card: "rgba(0,0,0,0.06) 0px 2px 8px"
    header: "rgba(0,0,0,0.06) 0px 1px 4px"
    modal: "rgba(0,0,0,0.18) 0px 8px 32px"
  components_harvested: true
  components:
    button-primary: { type: button, bg: "#e8552d", fg: "#ffffff", radius: "8px", padding: "12px 24px", font: "16px / 600", hover: "bg #d14a26", use: "주요 액션 (질의 실행, 피드백 저장, 체크 완료)" }
    button-secondary: { type: button, bg: "#ffffff", fg: "#1f1f1f", border: "1px solid #d4d4d4", radius: "8px", padding: "12px 24px", font: "16px / 600", hover: "bg #f7f7f7", use: "보조 액션 (수정, 나중에)" }
    pos-tile: { type: card, bg: "#ffffff", fg: "#1f1f1f", radius: "8px", padding: "12px", font: "16px / 600", active: "bg #fdede7, 1px solid #e8552d", use: "체크리스트 인스턴스의 탭 가능한 항목 그리드 (실행 중 조회 화면)" }
    input: { type: input, bg: "#ffffff", fg: "#1f1f1f", border: "1px solid #d4d4d4", radius: "8px", padding: "12px 14px", font: "16px / 400", focus: "border #e8552d", states: "error border #e0353b", use: "자연어 질의창, 피드백 자유 서술, 템플릿/예식 등록 폼" }
    card-feature: { type: card, bg: "#ffffff", border: "1px solid #e6e6e6", radius: "8px", padding: "24px", use: "온보딩 설명, 관리자 기능 소개 카드" }
    card-order: { type: card, bg: "#ffffff", radius: "8px", padding: "12px", use: "예식 목록 / 체크리스트 인스턴스 카드, 상태색 좌측 보더" }
    badge-status: { type: badge, bg: "#1fa463", fg: "#ffffff", radius: "4px", padding: "2px 8px", font: "12px / 600", states: "진행중 #f5a623, 관련사례없음 #2b82e0", use: "체크리스트/예식/질의 상태" }
---

# Design System — 웨딩체크 (Inspired by iCHEF)

## 1. Visual Theme & Atmosphere

이 시스템은 iPad(태블릿) 기반의 웨딩홀 스캔 오퍼레이터 인수인계 도구이며, 그 설계 철학은 드문 이중 정체성을 반영한다: **라이브 예식을 진행할 만큼 신뢰할 수 있어야 하고**, 동시에 **불안한 신입 오퍼레이터에게 파트너처럼 따뜻하게 느껴져야 한다.** 분위기는 **깔끔한 운영적 온기(clean operational warmth)** — 밝고 정돈된 흰색 캔버스 위에, 확신을 주는 오렌지-레드(`#E8552D`)가 시각적 중심을 잡는다. 이 색은 예식장의 조명·촛불 같은 따뜻함과, 신입이 수천 번 누르게 될 "실행" 버튼의 행동력을 동시에 담는다. 차갑고 관료적인 엔터프라이즈 소프트웨어와 달리 이 시스템은 따뜻하고 인간적이며, 가벼운 소비자 앱과 달리 침착하고 신뢰할 수 있다.

타이포그래피는 깔끔하고 현대적인 산세리프(`Pretendard`, `Apple SD Gothic Neo`)로, 주 사용자가 예식 진행 중 태블릿을 훑어보는 오퍼레이터이기 때문이다. 위계는 **기능적이고 가독성 중심** — 명확한 크기, 넉넉한 대비, 빠르게 진행되는 예식 현장에서 한눈에 훑을 수 있어야 한다. 관리자(대표) 화면은 실제 체크리스트 시연 영상과 예식 데이터를 근거로 설명하는 반면, 실행 중 조회 화면은 고가독성 운영 인터페이스다 — 큰 탭 타깃, 명확한 상태, 신입을 늦추는 장식은 없다.

이 시스템을 구분 짓는 것은 **소비자가 아니라 오퍼레이터를 위한 설계**라는 원칙이다. 오렌지-레드는 브랜드이자 행동 색이다 — CTA, 핵심 버튼, 체크리스트 그리드의 선택 상태 — 나머지는 중립적이고 침착하게 유지되어, 한 번의 예식 내내(때로는 하루 여러 타임) 화면을 봐야 하는 사람이 피로해지지 않도록 한다. 이것은 환대(hospitality)의 마음을 가진 시스템이다 — 불안한 신입을 초대할 만큼 따뜻하고, 라이브 예식의 실행을 맡길 만큼 엄격하다.

**Key Characteristics:**
- **오렌지-레드(`#E8552D`)**를 브랜드 + 액션 색으로 — 따뜻함과 행동을 한 색에
- 깔끔한 흰색 운영 캔버스 — 밝고 침착하며, 긴 예식 세션에도 피로하지 않음
- 한국어 우선 타이포그래피(`Pretendard` / `Apple SD Gothic Neo`) — 국내 웨딩홀 오퍼레이터 대상
- 높은 완성도 기준(iCHEF 레퍼런스의 iF Gold/German Design Award 수준 크래프트 계승) — 대충 만든 "사내 툴" 느낌 배제
- 이중 표면: 따뜻한 관리자 온보딩(실제 시연 영상, 체크리스트 컨텍스트) vs. 고가독성 실행 화면
- 큰 탭 타깃 + 명확한 상태 — 빠르게, 태블릿으로, 예식 중 사용하도록 설계
- 기능적 위계 — 표현성보다 가독성; 어떤 것도 신입을 늦춰서는 안 됨
- 중립 그레이 스케일로 오렌지-레드가 주의를 독점하지 않도록
- 신뢰하지만 따뜻한 톤 — 선임이 옆에서 알려주듯, 관료적 시스템처럼이 아니게
- 보수적인 라운딩(`8px` 워크호스)로 깔끔하고 신뢰감 있는 느낌

## 2. Color Palette & Roles

> **Note:** 라이브 사이트가 없는 내부 프로젝트이므로, 아래 값은 iCHEF 레퍼런스 DESIGN.md의 토큰을 그대로 계승한다. 실제 프로덕트가 만들어지면 브랜드 색 재검토를 권장한다 (`use canonical` 유지 중).

### Primary (Brand / Action)
- **오렌지-레드** (`#E8552D`): 브랜드 + 주요 액션 색. 주요 CTA("질의하기", "피드백 저장", "체크 완료"), 핵심 실행 버튼, 활성/선택 상태.
- **오렌지-레드 Hover** (`#D14A26`): 눌림/호버 상태.
- **오렌지-레드 Tint** (`#FDEDE7`): 선택된 행, 강조 표면을 위한 옅은 워시.

### Surface & Background
- **Pure White** (`#FFFFFF`): 기본 콘텐츠 + 카드 표면.
- **Surface Soft** (`#F7F7F7`): 섹션 그룹, 페이지 틴트, 체크리스트 배경 패널.
- **Surface Hover** (`#EFEFEF`): 호버/눌림 중립 표면.

### Neutral / Text
- **Text Primary** (`#1F1F1F`): 제목, 핵심 라벨, 주요 수치.
- **Text Secondary** (`#555555`): 본문, 설명.
- **Text Muted** (`#888888`): 메타데이터, 캡션, 헬퍼 텍스트.
- **Text Disabled** (`#BCBCBC`): 비활성 라벨, 플레이스홀더.

### Borders & Dividers
- **Border Light** (`#E6E6E6`): 행 구분선, 옅은 구분자.
- **Border Mid** (`#D4D4D4`): 컴포넌트 보더(입력창, 아웃라인 버튼, 카드).

### Semantic (operational — 안전장치의 핵심)
- **Success / 완료** (`#1FA463`): 체크리스트 항목 완료, 피드백 저장 완료, 질의에 근거 있는 사례가 매칭됨. 초록 = 확실한 결과.
- **Warning / 진행중·임시저장** (`#F5A623`): 예식 진행 중, 피드백 임시저장(선임이 나중에 이어 쓰기로 한 상태).
- **Error / 실패** (`#E0353B`): 입력 오류, 질의 응답 실패(네트워크 등), 삭제/취소 액션.
- **Info / 관련 사례 없음** (`#2B82E0`): 질의에 매칭되는 과거 변수 케이스가 없을 때. **이 색은 실패가 아니라 정직함의 신호다** — 억지로 엉뚱한 사례를 근거처럼 보여주지 않는다는 이 시스템의 핵심 안전장치.

## 3. Typography Rules

### Font Stack (locale-aware)
| Locale | Stack |
|---|---|
| 기본(한국어) | `Pretendard, "Apple SD Gothic Neo", "Malgun Gothic", -apple-system, sans-serif` |
| 영문(관리자 리포트 등 필요 시) | `-apple-system, "Segoe UI", Roboto, Helvetica, Arial, sans-serif` |

iCHEF가 대만/싱가포르/말레이시아 시장에 로케일별 네이티브 폰트 스택을 쓰던 원칙을 그대로 계승해, 이 시스템은 국내 웨딩홀이라는 단일 로케일에 맞춰 한국어 네이티브 스택(`Pretendard` 우선)을 쓴다.

### Weights
- **700 (Bold)**: 헤드라인, 핵심 수치(인사이트 반복 횟수, 예식 건수), 주요 CTA.
- **600 (Semibold)**: 부제목, 버튼 라벨, 활성 탭, 체크리스트 항목명.
- **400 (Regular)**: 본문, 설명, 메타데이터.

### Size Scale (px)
| Use | Surface | Size |
|---|---|---|
| 캡션 / 메타 | 공통 | `12–13px` |
| 본문 | 공통 | `14–16px` |
| 체크리스트 항목 / 질의 카드 | 실행 화면 | `16–18px` (큰 탭 텍스트) |
| 인사이트 반복 횟수 / 통계 | 관리자 | `20–28px` (굵게, 스캔 가능) |
| 카드 / 섹션 제목 | 관리자 | `20–24px` |
| 대시보드 헤드라인 | 관리자 | `28–40px` |

### Conventions
- **실행 화면(태블릿) 텍스트는 일반 SaaS보다 크게** — 예식 중 팔 길이 거리에서 읽혀야 한다.
- **숫자(반복 횟수, 예식 건수)는 두드러지고 굵게** — 인사이트의 운영적 진실이다.
- **굵기 + 색이 위계를 만든다**; 오렌지-레드는 액션에만, 나머지는 중립.
- **한국어가 1순위** — 영문 라벨은 관리자 리포트 등 필요한 곳에만 보조적으로.

## 4. Component Stylings

### Buttons

**Primary (CTA / 실행 액션)**
- Background: `#E8552D` / Text: `#FFFFFF` / Border: none / Radius: `8px` / Padding: `12px 24px` / Font: `16px / 600`
- Hover: bg `#D14A26`
- Use: "질의하기", "피드백 저장", "체크 완료", 관리자의 "템플릿 등록" 등 핵심 액션

**Secondary (Outlined)**
- Background: `#FFFFFF` / Text: `#1F1F1F` / Border: `1px solid #D4D4D4` / Radius: `8px` / Padding: `12px 24px` / Font: `16px / 600`
- Hover: bg `#F7F7F7`
- Use: "나중에", "수정", "취소"

**체크리스트 항목 타일 (POS Tile 계승)**
- Background: `#FFFFFF`(또는 옅은 카테고리 틴트), 선택 = `#FDEDE7` + `1px solid #E8552D`
- Text: `#1F1F1F` / Radius: `8px` / Padding: `12px` / Font: `16px / 600`
- Use: 실행 중 조회 화면의 탭 가능한 체크리스트 항목 그리드 — 큰 타깃, 명확한 선택 상태

### Inputs

**기본**
- Background: `#FFFFFF` / Text: `#1F1F1F` / Border: `1px solid #D4D4D4` / Radius: `8px` / Padding: `12px 14px` / Font: `16px / 400`
- Focus: border `#E8552D` / Error: border `#E0353B`
- Use: 자연어 질의창, 피드백 자유 서술 입력, 템플릿/예식 등록 폼

**자연어 질의창 / 피드백 서술창**
- 위와 동일 스타일이되, 여러 줄(textarea)로 확장 가능
- placeholder는 "정해진 형식 없이 있었던 일을 그대로 적으세요"류의 톤 — 폼처럼 보이지 않아야 함(§10 참고)

### Cards

**Feature / Content Card (온보딩·관리자 설명)**
- Background: `#FFFFFF` / Border: `1px solid #E6E6E6` 또는 그림자 분리 / Radius: `8–12px` / Padding: `24px`
- Use: 기능 설명, 관리자 온보딩, 인사이트 항목 카드

**예식 / 체크리스트 인스턴스 카드**
- Background: `#FFFFFF`, 상태색 좌측 보더 / Radius: `8px` / Padding: `12px`
- Use: 오늘의 예식 목록, 체크리스트 인스턴스 — 상태색(초록/주황)으로 한눈에 진행 상황 파악

### Badges & Chips

**Status Badge**
- 완료: `#1FA463` bg / white
- 진행중·임시저장: `#F5A623` bg / `#1F1F1F`
- 관련사례없음: `#2B82E0` bg / white
- Radius: `4px` / Padding: `2px 8px` / Font: `12px / 600`
- Use: 체크리스트/예식/질의 상태 — 운영적 진실을 한눈에

### Tables
- 관리자 인사이트/피드백 목록은 깔끔한 테이블: `#E6E6E6` 행 구분선, 굵은 합계/횟수 행
- 얼룩말 줄무늬는 선택적으로 `#F7F7F7`

### Navigation
- 관리자: 흰색 스티키 헤더, 로고 좌측, 템플릿/예식/인사이트 내비, 오렌지-레드 "새 예식 등록" CTA 우측
- 실행 화면(태블릿): iPad 최적화된 사이드/하단 내비, 큰 아이콘(체크리스트 / 질의 / 피드백)

## 5. Layout Principles

### Structure
- **관리자(웹/데스크톱 겸용)**: 중앙 정렬 max-width(~1200px), 흰색/`#F7F7F7` 교차 섹션
- **실행 화면(태블릿)**: 고정 운영 레이아웃 — 상시 내비 + 체크리스트 영역 + 질의창; 한 손으로 빠르게 쓰도록 설계

### Spacing
- 8px 기반 스페이싱 스케일
- 실행 화면은 넉넉한 터치 스페이싱(≥44px 타깃); 관리자 화면은 편안한 섹션 리듬(48–80px)

### Density
- **관리자: 중간 밀도** — 편안하고, 시연 영상·데이터 중심, 안심시키는 느낌
- **실행 화면: 목적 중심 밀도** — 체크리스트 전체 + 질의창을 한 화면에 담되, 큰 탭 타깃으로 오탭을 방지

## 6. Depth & Elevation

이 시스템은 **깔끔하고 부드러운 기능적 그림자**를 유지한다 — 신뢰할 수 있어야지 화려할 필요는 없는 도구이기 때문이다.

- **카드 그림자**: `0 2px 8px rgba(0,0,0,0.06)` — 미세한 들뜸
- **스티키 헤더**: 스크롤 시 `0 1px 4px rgba(0,0,0,0.06)`
- **모달 / 다이얼로그**: `0 8px 32px rgba(0,0,0,0.18)`
- **체크리스트 활성 타일**: 그림자 없음 — 선택은 틴트 + 오렌지-레드 보더로 표시
- 버튼은 플랫; 오렌지-레드 색 자체가 무게감을 준다.

### Z-Index
- 스티키 내비가 콘텐츠 위; 모달이 크롬 위; 토스트/알림이 최상위(질의 실패나 오프라인 경고는 항상 드러나야 함).

## 7. Do's and Don'ts

- **DO** 오렌지-레드(`#E8552D`)를 브랜드 + 액션 전용으로 — CTA, 핵심 실행 버튼, 선택 상태.
- **DON'T** 실행 화면을 오렌지-레드로 도배하지 말 것 — 긴 예식 세션 내내 침착하고 피로하지 않아야 한다.
- **DO** 상태색을 엄격히 사용: 초록 = 완료/매칭됨, 주황 = 진행중/임시저장, 파랑 = 관련 사례 없음(정직한 신호).
- **DON'T** 브랜드 오렌지-레드와 에러 레드를 혼동하지 말 것 — 예식 중 한눈에 구분되어야 한다.
- **DO** 실행 화면에서 큰 텍스트와 ≥44px 탭 타깃을 쓸 것.
- **DON'T** 태블릿 컨트롤을 소비자 앱 크기로 줄이지 말 것 — 예식 중 오탭은 되돌릴 수 없다.
- **DO** 관리자 온보딩은 실제 체크리스트 시연 영상과 구체적 성과(교육 기간 단축 횟수)로 이끌 것.
- **DON'T** 추상적인 스톡 이미지를 쓰지 말 것 — 이 시스템의 신뢰는 실제 현장(부스, 장비, 시연 영상)에서 나온다.
- **DO** 인사이트 반복 횟수, 예식 건수 같은 숫자를 굵고 두드러지게.
- **DON'T** 실행 화면을 장식하지 말 것 — 신입을 늦추거나 산만하게 하는 것은 무엇도 안 된다.
- **DO** 한국어를 1순위로.
- **DO** 완성도의 기준을 높게 유지할 것 — 정렬, 스페이싱, 상태 디자인은 정밀해야 한다.

## 8. Responsive Behavior

### Breakpoints
| Width | Behavior |
|---|---|
| Desktop `>1200px` | 관리자: 전체 내비, 다단 카드/테이블 그리드, 중앙 정렬 컨테이너 |
| Laptop `1024–1200px` | 2–3열 그리드, 축약 내비 |
| Tablet / iPad `768–1024px` | **실행 화면의 네이티브 존** — 고정 체크리스트+질의 레이아웃; 관리자 화면은 1–2열로 축소 |
| Mobile `<768px` | 관리자: 단일 컬럼, 햄버거 내비; 실행 화면은 태블릿 우선(폰은 보조/확인용) |

### Touch & Mobile
- 실행 화면은 iPad 최적화: 큰 타일, 상시 노출되는 질의 진입점, 엄지로 닿는 액션
- 관리자 인사이트/리포트 화면은 모바일에서도 확인 가능(대표가 외부에서 모니터링)

### Media
- 시연 영상은 `object-fit: cover`, 일관된 종횡비
- 체크리스트 항목 카드에 영상 썸네일 + 재생 아이콘

## 9. Agent Prompt Guide

### Quick Color Reference
- 브랜드 / 액션: 오렌지-레드(`#E8552D`); 호버 `#D14A26`; 틴트 `#FDEDE7`
- 텍스트 기본: `#1F1F1F`; 뮤트: `#888888`
- 표면: 흰색 `#FFFFFF`; 소프트 `#F7F7F7`
- 보더: `#D4D4D4`
- 상태: 완료 `#1FA463` · 진행중/임시저장 `#F5A623` · 실패/에러 `#E0353B` · 관련사례없음 `#2B82E0`

### Example Component Prompts
- "체크리스트 항목 타일을 만들어줘: 흰 배경, 8px 라운드, 12px 패딩, 항목명 16px/600 #1F1F1F, ≥44px 높이. 선택 상태: `#FDEDE7` 배경 + 1px solid #E8552D. 큰 탭 타깃."
- "자연어 질의 응답 카드를 만들어줘: 흰 배경, 1px solid #E6E6E6, 12px 라운드, 24px 패딩. 상단에 유사도 상위 사례, 상황 설명 14px/400 #555555, 사후 판단(대처법+근거) 강조. 관련 사례 없으면 `#2B82E0` 톤의 '관련 사례 없음 — 선임에게 연락하세요' 카드로 대체(절대 억지 매칭 금지)."
- "예식/체크리스트 인스턴스 카드를 만들어줘: 흰 배경, 8px 라운드, 상태색 좌측 보더(초록=완료/주황=진행중), 예식 시간 20px/700, 항목 진행률 굵게, 상태 배지 우상단."
- "피드백 입력창을 만들어줘: 정해진 폼 필드 없이 자유 서술 textarea 하나, placeholder '있었던 일을 그대로 적으세요', 저장 시 시스템이 5개 필드로 자동 구조화한 미리보기를 보여주고 확인/수정 가능."

### Iteration Guide
1. **오렌지-레드는 액션; 실행 화면은 침착하게.** 긴 예식 세션은 피로 저항적인 중립 베이스를 요구한다.
2. **상태색은 엄격하게:** 초록=완료, 주황=진행중/임시저장, 파랑=관련사례없음(안전장치) — 브랜드 오렌지-레드와 구분.
3. **실행 화면에서 큰 텍스트 + ≥44px 타깃.** 예식 중 오탭은 되돌릴 수 없다.
4. **숫자(인사이트 반복 횟수, 예식 건수)는 굵고 두드러지게.**
5. **관리자 온보딩 = 실제 시연 영상 + 구체적 성과**, 추상적 스톡 이미지 지양.
6. **8px 라운드 워크호스; 플랫 표면; 미세한 그림자.** 신뢰감, 화려함 아님.
7. **한국어가 1순위.**

---

## 10. Voice & Tone

이 시스템은 **선임이 옆에서 알려주는 것처럼** 말한다 — 실용적이고, 격려하며, 오퍼레이터 편에 서 있고, 절대 잘난 척하거나 전문용어로 채우지 않는다. 톤은 **선임-신입 파트너 관계**다: 예식 현장의 현실(조작, 변수 상황, 인수인계 기간)을 평이한 말로 다루고, 기술을 그 자체 목적이 아니라 더 나은 인수인계를 위한 수단으로 프레이밍한다. 이 프로젝트의 핵심 진단 — "문제는 하나가 아니라 둘이다. 조작법은 반복으로 숙달되지만 변수 대응력은 20회를 반복해도 잘 줄지 않는다" — 가 톤의 뼈대다: 자신감 있고, 정직하게 한계를 인정하며, 신입의 성공에 초점을 맞춘다. 한국어가 1순위 실행 화면 보이스이며, 관리자 리포트에 필요할 때만 영문을 보조적으로 쓴다. 관리자(온보딩) 보이스는 따뜻하고 구체적(실제 체크리스트, 실제 숫자)이고, 실행 화면 보이스는 간결하고 운영적이다(예식 중일 때는 명료함이 최우선).

| Context | Tone |
|---|---|
| 관리자 온보딩 | 자신감 + 대표 관점. "교육 기간을 줄이고, 우리 홀 전용으로." 성과 중심. |
| CTA | 구체적 + 초대하는. "체크리스트 등록하기", "예식 등록하기". 저압박. |
| 실행 화면 라벨 | 간결, 운영적. "체크 완료", "질의하기", "피드백 남기기". 동사 중심, 군더더기 없음. |
| 성과/가치 카피 | 구체적이고 근거 있음 — 동반 예식 횟수 감소, 반복 문제 축소. 형용사보다 숫자. |
| 빈 상태 | 실용적인 다음 행동 제시(첫 체크리스트 항목 등록 / 첫 예식 등록). 격려하는 톤. |
| 에러(운영) | 명확하고 즉각적 — 무슨 일이 있었는지, 무엇을 해야 하는지. 예식 중인 오퍼레이터를 탓하지 않음. |
| 관련 사례 없음 | 조용하지만 명확한 인정 — "관련 사례 없음 — 선임에게 연락하세요". 억지 근거를 만들지 않는다. |
| 성공(피드백 저장/체크 완료) | 조용한 확인; 상태색 자체가 신호. 예식 중 축하하는 연출 없음. |
| 지원/온보딩 | 인내심 있게, 손 잡아주듯 — 많은 사용자가 IT에 익숙하지 않다. |

**Forbidden phrases.** 오퍼레이터를 낮춰보는 기술 용어("시너지 플랫폼 활용"), 실수에 대한 가벼운 "앗!"류 표현, 근거 없는 과장("혁신적인 AI"), 추상적인 엔터프라이즈 화법, 예식 중 사용자를 탓하는 어떤 표현, 실행 화면에서의 과한 느낌표 마케팅 카피, 관련 사례가 없을 때 그럴듯하게 지어내는 답변.

**Voice samples.**
- `문제는 하나가 아니라 둘입니다 — 조작법은 영상으로, 변수 상황은 쌓인 경험으로.` — 이 시스템의 핵심 가치 제안 <!-- source: 제안서-웨딩홀 스캔 인수인계 시스템.md §핵심 진단, 2026-07-23 -->
- `지금 이런 상황인데 어떡해요?` — 신입이 실행 중 입력하는 질의의 실제 예시 <!-- source: PRD §2.3 UJ-1, 2026-07-23 -->
- `관련 사례 없음 — 선임에게 연락하세요.` — 근거 없는 답변을 만들지 않는다는 안전장치의 실제 표현 <!-- source: PRD §4.3 FR-6, §6 Safety, 2026-07-23 -->
- `있었던 일을 그대로 말해주세요.` — 피드백 입력을 폼이 아닌 자연어로 유도하는 톤 (원문서: "선임 본인도 변수 상황을 정형화해서 기억 못 함") <!-- source: 제안서 §2 ④, PRD §4.4, 2026-07-23 -->

## 11. Brand Narrative

이 시스템은 **2026년 7월 웨딩홀 현장 인터뷰**에서 출발했다. 신입 스캔(조명·음향·영상) 오퍼레이터의 인수인계에는 선임 동반 20~25회, 약 한 달이 걸리는데, 인터뷰로 확인된 사실은 반직관적이었다 — 홀마다 정해진 "조작법"은 몇 번만 반복해도 금방 숙달되지만, 예식마다 달라지는 "변수 상황"(주례자의 즉흥적인 순서 변경 등)에 대한 대응력은 20회를 반복해도 잘 줄지 않았다. 더 결정적인 사실은, **선임 본인도 그 변수 상황을 정형화해서 기억하지 못한다**는 점이었다 — "지금 당장은 생각이 잘 안 난다"는 답이 그 증거였다. 이는 이 시스템의 가장 중요한 설계 전제를 낳았다: 폼을 주면 아무도 쓰지 않는다. 자연어로, 말하듯 기록하게 해야 한다.

이 진단에서 나온 것이 "체크리스트 → 인스턴스 → 실행 → 피드백 → 집계"라는 구조다. 정형 조작법(체크리스트 템플릿, 시연 영상)은 순수 데이터로 다루고, 변수 상황 대응(실행 중 질의, 피드백, 인사이트)만 자연어와 의미 기반 매칭이라는 AI의 실제 가치를 쓴다. 이 원칙 — **AI가 필요한 곳에만 AI를 쓴다** — 은 이 프로젝트의 v1 스코프 전체를 관통한다.

이 시스템은 아직 하나의 웨딩홀을 위한 파일럿이다 (`[FILL IN: 실제 서비스명 확정 시 갱신]`). 성공의 기준은 화려함이 아니라, 신입이 혼자 부스에 있을 때 그 자리에서 근거 있는 답을 받을 수 있는지, 그리고 선임이 부담 없이 겪은 일을 남길 수 있는지다.

## 12. Principles

1. **오퍼레이터가 오퍼레이터를 위해 설계한다.** 이 시스템의 공감은 실제 인터뷰에서 나온다; UI는 현장의 현실을 존중한다. *UI implication:* 큰 탭 타깃, 간결한 운영 라벨, 핵심 액션(체크, 질의, 저장)으로의 빠른 경로. 예식 중인 오퍼레이터가 헤매거나 긴 글을 읽게 하지 않는다.

2. **따뜻해서 신뢰하고, 엄격해서 맡길 수 있다.** 오렌지-레드는 초대하고, 절제는 안심시킨다. *UI implication:* 브랜드/액션에는 따뜻한 오렌지-레드를, 근거·상태 표면에는 정확함을 — 애매한 상태 표시는 없다.

3. **부담 아래서도 침착하다.** 예식은 취소·재시도가 불가능한 라이브 이벤트다. *UI implication:* 중립적인 운영 캔버스, 오렌지-레드는 액션에만 배급; 장식적 애니메이션 없음; 표현성보다 고대비·가독성.

4. **근거는 신성하다.** 질의에 답할 때 관련 사례가 있는지 없는지의 차이는, 신입이 올바르게 대처하는지 아니면 확신 없이 즉흥적으로 행동하는지의 차이다. *UI implication:* 관련 사례가 없으면 반드시 "없음"을 명시하고 절대 임의 사례를 근거처럼 제시하지 않는다; 질의 실패는 즉시 드러내고 조용히 묻지 않는다.

5. **사내 툴이어도 완성도는 타협하지 않는다.** 원본 레퍼런스(iCHEF)의 수상 경력 수준 크래프트 기준을 계승한다. *UI implication:* 정렬, 스페이싱, 라운딩(8px), 상태 디자인의 정밀함. "사내 툴이니 대충"은 이 시스템의 기준이 아니다.

6. **한국어가 1순위.** 국내 단일 웨딩홀 대상이므로 모든 카피와 폰트 스택은 한국어 네이티브를 우선한다. *UI implication:* `Pretendard` 계열 스택, 한국어로 먼저 작성되는 카피.

## 13. Personas

*아래 페르소나는 PRD §2.3(Key User Journeys)에 기술된 실제 사용자 세그먼트를 바탕으로 한 허구의 대표 인물이며, 실존 인물이 아니다.*

**이서연, 24세, 입사 2주 차 신입 오퍼레이터.** 혼자 부스에 있고 선임은 다른 업무 중이다. 주례자가 사전 협의 없이 순서를 바꾸는 것 같은 낯선 상황에서 실수로 예식을 망칠지 모른다는 불안이 가장 크다. 실행 화면 하단 질의창에 "이런 상황인데 어떡하죠?"를 입력해 즉시 근거 있는 답을 받는 것이 그의 90% 사용 패턴이다. 큰 탭 타깃과 즉각적인 피드백을 중요하게 여긴다 — 예식 중 렉이 걸리거나 컨트롤이 작으면 그것이 그의 악몽이다.

**김도윤, 27세, 근속 1년 차 선임 오퍼레이터.** 매번 같은 설명을 말로 반복하지 않고, 겪은 변수 상황을 부담 없이 기록만 해두면 다음 신입에게 자동으로 전달되길 바란다. 예식 종료 후 정해진 폼을 채우는 대신 "있었던 일을 그대로" 자유 서술로 남긴다. 가끔 "지금 당장은 생각이 잘 안 난다"며 임시 저장하고 나중에 이어 쓴다.

**박태호, 52세, 웨딩홀 대표.** 홀마다 다른 워크플로우를 일반화하기 어려웠던 문제를 이번엔 "우리 홀 전용"으로 풀고 싶어 한다. 교육 기간 단축을 눈에 보이는 성과(동반 예식 횟수 감소)로 확인하고 싶어 하며, 반복되는 문제 원인을 인사이트 화면에서 한눈에 확인하는 것이 그의 주 사용 패턴이다.

## 14. States

| State | Treatment |
|---|---|
| **Empty (등록된 체크리스트 항목 없음)** | `#888888` 안내 문구 + 주요 CTA(`#E8552D`)로 첫 항목 등록. 격려하고 실용적. |
| **Empty (오늘 등록된 예식 없음)** | 차분한 `#888888` 안내 + "예식 등록" 액션. 조용한 날의 기본 화면. |
| **Loading (인사이트 클러스터링 갱신 중)** | `#F7F7F7` 스켈레톤 블록, 8px 라운드; 기존 인사이트는 갱신 중에도 계속 보임(스피너가 화면을 덮지 않음). |
| **Loading (질의 응답 대기)** | 질의 버튼은 너비 유지, 버튼 내 스피너; 중복 질의 방지. 예식 중 두 번 눌러도 중복 요청되지 않아야 한다. |
| **Error (입력 필드)** | `1px solid #E0353B` 보더 + 12px `#E0353B` 헬퍼 텍스트, 필드별로 구체적이고 탓하지 않는 문구. |
| **Error (질의 응답 실패 — 네트워크 등)** | 즉시 드러나는 알림(조용한 토스트 아님) — `#E0353B` 강조, 무슨 일이 실패했는지 + 재시도 문구. 실패는 항상 드러나야 한다. |
| **관련 사례 없음 (에러 아님, 안전장치)** | `#2B82E0` 톤의 명확한 카드 — "관련 사례 없음 — 선임에게 연락하세요". 절대 그럴듯한 답을 대신 지어내지 않는다. |
| **Error (오프라인 / 동기화)** | 지속적인 배너: 체크리스트 조회는 로컬 캐시로 계속 동작하며, 오프라인이어도 저장되고 나중에 동기화됨을 안심시킴. AI 질의만 영향을 받고, 예식 진행 자체를 막지 않는다. |
| **Success (피드백 저장 / 체크 완료)** | 상태가 초록(`#1FA463`)으로 바뀜; 조용한 확인. 초록 상태 배지 자체가 신호 — 예식 중 축하 애니메이션 없음. |
| **Success (폼 저장)** | 짧은 확인 문구 + 흐름으로 복귀. |
| **Skeleton** | 정확한 크기의 `#F7F7F7` 블록; 숫자 플레이스홀더는 `—`로, `0`으로 표시하지 않음(집계 전과 0건을 혼동시키지 않기 위해). |
| **Disabled (버튼)** | 흐린 채움, `#BCBCBC` 텍스트, 기하학적 크기 유지(재활성화 시 레이아웃이 흔들리지 않도록). |

## 15. Motion & Easing

이 시스템의 모션은 **최소한이고 운영적**이다 — 피드백은 즉각적이고 명확해야 하며, 어떤 것도 예식 중 진행을 지연시키거나 산만하게 해서는 안 된다.

**Durations:**
| Token | Value | Use |
|---|---|---|
| `motion-instant` | 0ms | 체크리스트 항목 선택, 토글, reduce-motion 대체 |
| `motion-fast` | 120ms | 호버/눌림, 탭 전환, 작은 노출 |
| `motion-standard` | 200ms | 드롭다운, 패널 슬라이드, 툴팁 페이드 |
| `motion-slow` | 300ms | 모달 열림, 설정 드로어 |

**Easings:**
| Token | Curve | Use |
|---|---|---|
| `ease-standard` | `cubic-bezier(0.4, 0, 0.2, 1)` | 기본 양방향 전환 |
| `ease-enter` | `cubic-bezier(0, 0, 0.2, 1)` | 나타나는 것 — 모달, 시트 |
| `ease-exit` | `cubic-bezier(0.4, 0, 1, 1)` | 사라지는 것 |

**Spring stance.** 실행 화면에서 스프링/오버슈트 모션은 **금지**된다. 이 시스템은 라이브 예식이라는 되돌릴 수 없는 순간을 다루는 도구이며, 통통 튀는 모션은 피드백을 지연시키고 신뢰를 해친다. 체크리스트 항목 선택은 **즉각적**이다(0ms) — 탭이 즉시 등록되어야 한다. 예식 중 인지되는 지연은 곧 사용성 실패이기 때문이다. 관리자 온보딩 화면은 부드러운 스크롤 노출 정도는 쓸 수 있으나, 탄성 모션은 금지한다.

**Signature motions.**
1. **체크리스트 항목 탭.** 즉각적인 상태 변화 — 타일이 `#FDEDE7` 틴트 + 오렌지-레드 보더를 0ms 지연으로 얻는다. 손끝이 곧 피드백이다; 탭과 등록 사이에 애니메이션이 끼어들지 않는다.
2. **질의 응답 도착.** 새 사례 카드가 `motion-fast / ease-enter`로 슬라이드인 된다; 유사도 순위는 즉시 표시된다(카운트업 애니메이션 없음 — 숫자는 바뀌는 즉시 정확해야 한다).
3. **상태 전환.** 항목/예식이 완료로 전환되면 `motion-standard`로 초록으로 크로스페이드된다 — 빠르고 명확하되, 축하하지 않는다.
4. **모달 / 설정 드로어.** `motion-slow / ease-enter`로 슬라이드인; `ease-exit`로 닫힘. 표준적이고 차분함.

**Reduce motion.** `prefers-reduced-motion: reduce` 하에서는 모든 전환이 `motion-instant`로 축소된다; 모달/드로어 슬라이드는 즉시 opacity 토글이 된다. 운영적 피드백은 영향받지 않는다(이미 즉각적이었으므로). 예외 없음.

<!--
OmD v0.1 Sources — 웨딩체크 (bootstrapped from ichef)
Created: 2026-07-23

Base reference: .claude/data/references/ichef/DESIGN.md (verified 2026-05-19, see that file for iCHEF's own
Tier 1/Tier 2 source notes). Token values (colors/typography/spacing/radius/shadow) carried over unchanged —
no delta_set overrides were requested at bootstrap time.

Project facts (§11 Brand Narrative, §12 Principles, §13 Personas, Voice samples in §10): sourced directly from
this project's own documents, not fabricated —
- 제안서-웨딩홀 스캔 인수인계 시스템.md (2026-07-23 인터뷰 진단서)
- _bmad-output/planning-artifacts/prds/prd-wedding-check-2026-07-23/prd.md (2026-07-23, status: final)

Assumptions carried from PRD §12 Assumptions Index still apply and are not restated here — see prd.md.
Project/brand name is a working title pending client confirmation (see PRD title note "Working title — confirm.").
Personas (§13) are fictional composites built from PRD §2.3 UJ personas, not individual real people.
-->

---

**Bootstrapped from:** ichef (`.claude/data/references/ichef/DESIGN.md`, verified 2026-05-19)
**Project sources:** 제안서-웨딩홀 스캔 인수인계 시스템.md · prd.md (both 2026-07-23)
**Mode:** inspired (no live reference-capture; tokens carried over from iCHEF's canonical DESIGN.md)
**Open items:** 프로젝트/서비스명 확정 필요 (working title) · PRD §11 Open Questions 참고
