# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (secrets — never commit; .example templates are safe and tracked, AD-10)
.env*
!.env*.example

# vercel
.vercel

# Story 1.4 로컬 영상 업로드 폴백 저장소(BLOB_READ_WRITE_TOKEN 없을 때) — 바이너리라 git 제외
.local-blob/

# typescript
*.tsbuildinfo
next-env.d.ts
