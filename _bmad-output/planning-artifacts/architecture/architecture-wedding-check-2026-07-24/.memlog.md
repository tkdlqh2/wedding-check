---
scope: 웨딩홀 스캔 오퍼레이터 인수인계 시스템 v1 — full system architecture (feature altitude)
purpose: build-substrate
altitude: feature
updated: 2026-07-24T17:57
---

- (decision) Stack: Next.js full-stack (App Router, TS) on Vercel, single deploy for admin+tablet PWA. Data: Postgres+pgvector on same instance for both operational tables and embedding search (no separate vector DB) -- adopted per user choice.
- (decision) Offline strategy: lightweight client-side memory/localStorage cache of last-loaded checklist instance, NOT full PWA/service-worker. User explicitly chose this over full offline support, accepting it only partially satisfies PRD SS5 availability NFR (won't survive a cold load while offline). Flag as known gap.
- (decision) LLM/embeddings vendor (web-verified 2026-07-24): embeddings = voyage-3.5 (Anthropic's recommended embedding partner; Matryoshka 2048/1024/512/256-dim; benchmarked on 26 languages incl. Korean; $0.06/1M tok) at 1024-dim in pgvector. Generation: claude-sonnet-5 for FR-6/7 real-time grounded query answering (low query volume, honesty/safety-critical, output_config.effort low/medium, structured output for citation+verdict). claude-haiku-4-5 for FR-9/10 daily batch feedback structuring (5 fields+tag) via Message Batches API (50% off); clustering itself is embedding+cosine similarity, not an LLM call.
- (decision) Roles: 2-role model, not 3 -- 오퍼레이터 (merged former 신입+선임, full query+feedback access) and 관리자/대표 (hall+template+ceremony+insight admin). Synced back to PRD (prd.md updated 2026-07-24).
- (decision) Auth: Better Auth (not NextAuth/Auth.js v5) -- web-verified 2026-07-24 that Auth.js maintainers now direct new/greenfield projects to Better Auth (Auth.js itself is now maintained by the Better Auth team). Supports Next.js App Router; suits simple credential/PIN login for tablet kiosk use.
- (decision) Hall data isolation: application-layer hall_id filtering in the repository layer, not Postgres RLS. Adopted per user choice given single-business/few-halls v1 scope; simpler to implement/debug. Deferred: RLS reconsideration if v2 multi-tenant (cross-business) expansion happens.
- (decision) Paradigm: layered Next.js monolith with explicit repository layer (lib/db/, hall_id-filtered) between services and Postgres, plus a Ports-and-Adapters boundary around AI (lib/ai/ports.ts: LLMPort, EmbeddingPort; adapters/anthropic.ts, adapters/voyage.ts) to satisfy PRD vendor-neutral requirement. Confirmed with user explicitly (asked whether layered includes a repository layer -- yes).
- (version) Stack versions to verify at Reviewer Gate: Next.js (App Router, latest stable), Postgres+pgvector (Neon via Vercel Marketplace per vercel:vercel-storage), voyage-3.5 embeddings, claude-sonnet-5 + claude-haiku-4-5 generation, better-auth.
- (change) Reviewer gate applied: tightened AD-1 (LLMPort/EmbeddingPort signatures pinned, ESLint import-boundary enforcement), AD-2 (checklist_instances denormalized hall_id, mandatory 2-hop hall re-validation on instance_item writes, naming table now enumerates all ERD entities), AD-4 (demo_videos row write only via onUploadCompleted server callback, not client-reported URL), AD-5 (stale-while-revalidate convention for online steady-state, closing the FR-5-edit-not-reaching-open-tablet gap). Added AD-7 (insight_clusters single-writer upsert + concurrency lock), AD-8 (variable_case created only on confirmed feedback -- closes safety-critical gap where draft/unverified auto-structured content could reach the FR-6/7 search index; also fixes ERD FEEDBACK-VARIABLE_CASE cardinality 1:1 -> 0..1), AD-9 (contract-type conditional item inclusion: JSONB applicable_contract_conditions column on checklist_template_items, not a normalized rule table), AD-10 (deployment/environments: Neon branch-per-Vercel-environment, CI-gated Drizzle migrations, Vercel Cron for FR-10 daily batch, structured logging for AI failures, per-environment secrets). Verified Next.js/Drizzle versions now cite verification provenance matching the other Stack rows. lint_spine.py clean (0 findings) after all edits.
- (event) spine finalized
- (change) Applied version-verify review's last open advisory: Stack table now documents rationale for choosing claude-sonnet-5/claude-haiku-4-5 over the claude-api skill's default claude-opus-4-8 (latency for FR-6/7 p95 5s, cost/complexity fit for FR-9 batch). All findings from all 3 reviewer-gate lenses (rubric-walker, adversarial, version-verify) are now fully closed. lint_spine.py re-run clean (0 findings).
